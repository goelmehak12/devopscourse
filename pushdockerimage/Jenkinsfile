pipeline {
    agent any
    environment {
        DOCKERHUB_CREDS = credentials('dockerhub')
    }
    stages {
        stage('Clone Repo') {
            steps {
                checkout scm
                sh 'ls -l'
                sh 'ls -l pushdockerimage'  // Optional: to see folder contents
            }
        }
        stage('Build Image') {
            steps {
                // Build Docker image using the root directory where Dockerfile is present
                sh 'docker build -t dockermehak/jenkinstest:$BUILD_NUMBER .'
            }
        }
        stage('Docker Login') {
            steps {
                sh 'echo $DOCKERHUB_CREDS_PSW | docker login -u $DOCKERHUB_CREDS_USR --password-stdin'
            }
        }
        stage('Docker Push') {
            steps {
                sh 'docker push dockermehak/jenkinstest:$BUILD_NUMBER'
            }
        }
    }
    post {
        always {
            sh 'docker logout'
        }
    }
}
